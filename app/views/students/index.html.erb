<p style="color: green"><%= notice %></p>

<nav class="navbar navbar-expand-lg bg-secondary" data-bs-theme="dark">
    <div class="container">
        <a class="navbar-brand" >My Portfolio</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
            aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button"
                        aria-haspopup="true" aria-expanded="false">Profile</a> <!-- Changed to display user name -->
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ url_for('account') }}">Sign In</a>
                        <a class="dropdown-item" href="{{ url_for('signout') }}">Sign Out</a> <!-- Made uniform with the rest of the navbar style -->
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>


<h1>Students</h1>

<div id="students">
  <% @students.each do |student| %>
    <%= render student %>
    <p>
      <%= link_to "Show this student", student %>
    </p>
  <% end %>
</div>

<%= link_to "New student", new_student_path %>
